{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Transpile",
            "type": "shell",
            "command": "tsc && python transpiler/replace_vex_lib.py && npm run rollup && python transpiler/transpile.py",
            "group": "test",
            "presentation": {
                "reveal": "always"
            }
        }
    ]
}
